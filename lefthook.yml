pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: bun run biome check --write {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bun run commitlint --edit {1}
